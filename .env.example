POSTGRES_USER=activitylog
POSTGRES_PASSWORD=changeme
POSTGRES_DB=activitylog

# Use these when running locally (outside Docker)
DATABASE_URL=postgresql+asyncpg://activitylog:changeme@localhost:5432/activitylog
REDIS_URL=redis://localhost:6379/0

# Use these when running inside Docker (docker compose overrides these automatically)
# DATABASE_URL=postgresql+asyncpg://activitylog:changeme@postgres:5432/activitylog
# REDIS_URL=redis://redis:6379/0

CORS_ORIGINS=http://localhost:5500
